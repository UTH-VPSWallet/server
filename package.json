{
	"name": "server",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --local",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"seed": "wrangler d1 migrations apply --local server_db"
	},
	"dependencies": {
		"hono": "^4.12.8"
	},
	"devDependencies": {
		"@types/node": "^25.5.0",
		"wrangler": "^4.75.0"
	}
}